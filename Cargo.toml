[package]
name = "dilithium-poc"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []
pkix = []
experimental-params = []
instrumentation = []

[dependencies]
sha3 = "0.10"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "sampling"
harness = false
